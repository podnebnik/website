[project]
name = "podnebnik-temperature-source"
version = "0.1"
description = "Podnebnik temperatures data source"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy==2.*",
    "pandas==3.*",
    "scipy == 1.*",
    "netcdf4",
]

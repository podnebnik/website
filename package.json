{
  "private": true,
  "scripts": {
    "clean": "rm -rf dist && dotnet fable clean",
    "start": "npx @11ty/eleventy --serve --incremental --quiet",
    "start-debug": "DEBUG=Eleventy* npx @11ty/eleventy --serve --incremental --quiet",
    "build": "npx @11ty/eleventy",
    "build-debug": "DEBUG=Eleventy* npx @11ty/eleventy"
  },
  "dependencies": {
    "highcharts": "^10.3.3"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-plugin-vite": "^4.0.0",
    "@11ty/is-land": "^3.0.1",
    "sass": "^1.58.0",
    "solid-element": "^1.6.3",
    "solid-js": "^1.6.10",
    "vite-plugin-solid": "^2.5.0"
  }
}
